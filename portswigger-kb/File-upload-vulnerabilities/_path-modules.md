# File upload — learning path, full module walk (0 → end)

Walked live via CONTINUE to "You've completed File upload vulnerabilities" (35 units).

## Module groups → folder
1-3. What are file upload vulns / impact / how they arise → README
4-5. How web servers handle static files (exec by extension) → README
6-7. Exploiting unrestricted uploads → web shell + Lab RCE web-shell-upload → Basics-RCE
8-11. Flawed validation: file-type/Content-Type + Lab content-type-bypass → Basics-RCE
12-14. Preventing exec in user dirs (non-exec upload dir) + Lab path-traversal → Bypasses
15-20. Insufficient blacklisting: overriding server config (.htaccess), obfuscating extensions + Labs blacklist-bypass, obfuscated-extension → Bypasses
21-26. Flawed validation of file contents (magic bytes) + polyglot + Lab polyglot → Bypasses
27-30. Exploiting file upload race conditions + URL-based race + Lab race-condition → Race-condition
31-33. Exploiting WITHOUT RCE: malicious client-side scripts (SVG/HTML XSS), parser vulns (XXE) → README (Exploiting without RCE)
34. Uploading files using PUT → README (Exploiting without RCE)
35. How to prevent → README

**Path walked 0 → end: 35/35 → "You've completed File upload vulnerabilities".** All 7 labs covered (Basics-RCE 2, Bypasses 4, Race-condition 1).
